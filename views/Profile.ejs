<!DOCTYPE HTML>
<html lang="en/ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Profile</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <!-- Bootstrap Icons CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="icon" href="https://cdn-icons-png.flaticon.com/128/3054/3054889.png" type="icon/ico" style="width: auto; height: auto;">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-pzDp/i/3A7L+0ETe1jhjVnnXf5Xf2/K1b0P5g5+0Hov/vfZcO5NAhdxg79Y4TV0E" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0px;
            padding: 0px;
            overflow-x: hidden;
            background-color: #f9f9f9;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            background-color: #c4cca6;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
            background-color: #f9f9f9;
            padding: 10px 20px;
            border: 2px solid #ccc;
            border-radius: 8px;
            transition: background-color 0.3s ease;
            box-shadow: 2px 2px 2px #555;
            margin-bottom: 20px;
            max-width: 350px;
        }

        .profile-info {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .profile-info label {
            flex-basis: 30%;
            margin-bottom: 10px;
        }

        .profile-info input {
            flex-basis: 65%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .profile-details {
            flex-grow: 1;
            position: sticky;
            width: 100%;
            margin-top: 20px;
        }

        .profile-details p {
            margin: 10px 0px;
        }

        .profile-details strong {
            font-weight: bold;
            color: #555;
        }

        .profile-details {
            background-color: #f9f9f9;
            padding: 15px;
            border-radius: 8px;
            border: 2px solid #ccc;
        }
        /* Style for Configuration Icon */
        .config-icon {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 24px;
        }
        /* Custom styling for the form */
        .dropdown-item {
            border-radius: 30px;
            /* Adjust the width as needed */
            width: 300px; 
        }

        .dropdown-item label {
            font-weight: bold; /* Make labels bold */
        }

        .dropdown-item input[type="password"],
        .dropdown-item button[type="submit"] {
            width: 100%; /* Make inputs and button full width */
        }

        /* Adjust spacing between form elements */
        .dropdown-item .mb-3 {
            margin-bottom: 15px;
        }
        /* Profile Details Styling */
        #userInfoForm p {
            background-color: #8B4513; /* Coffee themed color */
            border: 1px solid #A0522D; /* Coffee themed border */
            padding: 5px;
            margin: 5px 0;
            border-radius: 5px;
        }
            /* Profile Picture Form Styling */
        #profilePictureForm label,
        #profilePictureForm input[type="file"],
        #profilePictureForm button {
            background: linear-gradient(to right, #8B4513, #A0522D, #8B4513); /* Coffee themed linear background */
            border: none;
            padding: 5px;
            margin: 5px 0;
            color: white;
            border-radius: 5px;
        }
        #profileContainer #userInfoForm p {
            /* Coffee themed color */
            background-color: #8B4513; 
            /* Coffee themed border */
            border: 1px solid #A0522D; 
            padding: 5px;
            margin: 5px 0;
            border-radius: 5px;
        }
        #profileContainer #userInfoForm p strong {
            background: linear-gradient(to right, #8B4513, #A0522D, #8B4513); /* Coffee themed linear background */
            padding: 5px;
            border-radius: 5px;
        }
        #profileContainer #userInfoForm p span {
            background: linear-gradient(to right, #CD853F, #D2691E, #CD853F); /* Different linear background color */
            padding: 5px;
            border-radius: 5px;
            /* Max width set to 120px */
            max-width: 120px; 
             /* Hide overflow text */
            overflow: hidden;
            /* Add ellipsis for overflow text */
            text-overflow: ellipsis; 


        }
        /* Profile Picture Container Styling */
        .profile-picture-container {
            background: linear-gradient(to right, #cd863f7c, #d2691e77, #cd863f80); /* Coffee themed background */
            /* Set width */
            width: 100px; 
            /* Set height */
            height: 100px; 
            display: flex;
            align-items: center;
            justify-content: center;
            /* Make it a circle */
            border-radius: 50%; 
        }

        /* Profile Picture Styling */
        /* Retaining the ID selector */
        #profileImage { 
            /* Set width */
            width: 50px; 
            /* Set height */
            height: 50px; 
            /* Make it a circle */
            border-radius: 50%;
            background-color: whitesmoke; 
        }
        .form-control:focus + .form-label,
        .form-control:active + .form-label {
            color: #000; /* Set the color to black */
        }
        .mb-3 {
            height: 70px;
            margin-bottom: 10px; /* Adjust the margin bottom as needed */
        }
    </style>

    </head>
    <body>
        <div class="config-icon dropdown">
            <!-- Configuration Icon using Bootstrap Icon and Image -->
            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi bi-gear"></i>
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton" style="border-radius: 20px; max-height: 400px; overflow-y: auto;">
                <li>
                    <form class="dropdown-item p-4" id="resetPasswordForm">
                        <div class="mb-3">
                            <label for="currentPassword" class="form-label">Current Password</label>
                            <input type="password" class="form-control" id="currentPassword" required>
                        </div>
                        <div class="mb-3">
                            <label for="newPassword" class="form-label">New Password</label>
                            <input type="password" class="form-control" id="newPassword" required>
                        </div>
                        <div class="mb-3">
                            <label for="confirmPassword" class="form-label">Confirm New Password</label>
                            <input type="password" class="form-control" id="confirmPassword" required>
                        </div>
                        <button type="submit" class="btn btn-primary">Reset Password</button>
                    </form>
                </li>
            </ul>
        </div>
        <header>
            <!-- User Information -->
            <div class="container" id="profileContainer">
                <h1>Informatii Cont User</h1>
                <div class="profile-picture-container"> <!-- Changed class name -->
                    <!-- Profile Picture -->
                    <img id="profileImage" src="default-profile-image.jpg" alt="Profile Picture">
                    <!-- File input for uploading profile picture -->
                    <input type="file" id="profilePicture" name="profilePicture" accept="image/*" capture="environment" onchange="displaySelectedProfilePicture(event)">
                </div>
                <script>
                    // JavaScript function to display the selected profile picture
                    function displaySelectedProfilePicture(event) {
                        const selectedFile = event.target.files[0]; // Get the selected file
                        const profileImage = document.getElementById('profileImage'); // Get the profile image element

                        if (selectedFile) {
                            profileImage.src = URL.createObjectURL(selectedFile); // Set the src attribute to the temporary URL of the selected file
                        } else {
                            // If no file is selected, reset the src attribute to a default image or clear it
                            profileImage.src = 'default-profile-image.jpg'; // Replace 'default-profile-image.jpg' with the path to your default profile image
                        }
                    }
                </script>
            </div>
                <div class="profile-details">
                    <!-- User Details -->
                    <form id="userInfoForm" action="/user/profile" method="GET">
                        <p><strong>Username:</strong> <span id="username"></span></p>
                        <p><strong>Email:</strong> <span id="email"></span></p>
                        <p><strong>Location:</strong> <span id="location"></span></p>
                        <p><strong>Joined:</strong> <span id="joined"></span></p>
                    </form>
                    
                    <form id="profilePictureForm" enctype="multipart/form-data" action="/profile-photo" method="post">
                        <label for="profilePicture">Upload Profile Picture:</label>
                        <input type="file" id="profilePicture" name="profilePicture" accept="image/*" capture="environment">
                        <button type="button" onclick="uploadProfilePicture()">Upload</button>
                    </form>
                </div>
            </div>
        </header>
        <script>
            // Example client-side JavaScript using fetch API

            // Function to fetch user information
            async function fetchUserProfile() {
                try {
                    const response = await fetch('/user/profile');
                    if (response.ok) {
                        const user = await response.json();
                        // Update the profile page with user information
                        updateUserProfile(user);
                    } else {
                        console.error('Failed to fetch user profile:', response.statusText);
                    }
                } catch (error) {
                    console.error('An error occurred while fetching user profile:', error);
                }
            }

            // Function to update the profile page with user information
            function updateUserProfile(user) {
                document.getElementById('profileImage').src = user.profilePicture;
                document.getElementById('username').innerText = user.username;
                document.getElementById('email').innerText = user.email;
                document.getElementById('location').innerText = user.location;
                document.getElementById('joined').innerText = user.joined;
            }
            // Call the fetchUserProfile function when the page loads
            document.addEventListener('DOMContentLoaded', () => {
                fetchUserProfile();
            });
            //Fetching user information from the server
            fetch('/user/profile')
            .then(response => response.json())
            .then(data => {
                // Update profile information
                document.getElementById('profileImage').src = data.profilePicture;
                document.getElementById('username').textContent = data.username;
                document.getElementById('email').textContent = data.email;
                document.getElementById('location').textContent = data.location;
                document.getElementById('joined').textContent = data.joined;
            })
            .catch(error => {
                console.error('Error fetching user information', error);
            });
        </script>


        <main>
            <!-- Order History -->
            <section class="container">
                <h2>Order History</h2>
                <div class="order-history">
                    <!-- Display Orders with Dropdown for Details -->
                    <div class="order">
                        <p>Order #12345 - Status: Shipping</p>
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" id="orderDropdown1" data-bs-toggle="dropdown" aria-expanded="false">
                                View Details
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="orderDropdown1">
                                <li><a class="dropdown-item" href="#">Order Details</a></li>
                                <li><a class="dropdown-item" href="#">Track Order</a></li>
                            </ul>
                        </div>
                    </div>
                    <!-- More Orders... -->
                </div>
            </section>
            <style>
                .dropdown-menu {
                    /* Apply the wave animation */
                    animation: wave 0.55s ease-in-out;
                    /* Set the transform origin to the top */ 
                    transform-origin: top; 
                }

                @keyframes wave {
                    0% {
                        transform: scaleY(0); /* Start from fully collapsed */
                    }
                    100% {
                        transform: scaleY(1); /* Expand to full height */
                    }
                }

                .dropdown-item {
                    padding: 0.5rem 1rem;
                }

                .dropdown-item:hover {
                    background-color: #f8f9fa; /* Change background color on hover */
                }
                /* Coffee themed linear gradient */
                .dropdown-list {
                    background: linear-gradient(to right, #CD853F, #D2691E, #CD853F);
                }
                /* Bootstrap styled dropdown items */
                .dropdown-item {
                    background: linear-gradient(to right, #7a5d46, #5d3e2f, #7e7e28); /* Coffee themed light gradient */
                    /* color: white;  */
                }
                .dropdown-item:hover {
                    background-color: #A0522D; /* Darker shade on hover */
                }
            </style>
            <!-- Wishlist -->
            <nav class="container wishlist-container bg-gradient">
                <h2 class="border-bottom border-dark pb-2">Wishlist</h2>
                <div class="container mt-5">
                    <div class="dropdown">
                        <button class="btn btn-primary dropdown-toggle" type="button" id="wishlistDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            View Wishlist
                        </button>
                        <div class="dropdown-menu dropdown-list" aria-labelledby="wishlistDropdown">
                            <!-- Wishlist Items -->
                            <ul class="dropdown-list">
                                <li><a class="dropdown-item" href="#Link-Produse" onclick="event.stopPropagation()">Product 1</a></li>
                                <li><a class="dropdown-item" href="#Link-Produse" onclick="event.stopPropagation()">Product 2</a></li>
                                <li><a class="dropdown-item" href="#Link-Produse" onclick="event.stopPropagation()">Product 3</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </nav>
            <script>
                // Manually initialize the dropdown menu
                document.addEventListener('DOMContentLoaded', function() {
                    // Find the dropdown toggle button
                    var dropdownToggle = document.getElementById('wishlistDropdown');
            
                    // Find the dropdown menu
                    var dropdownMenu = document.querySelector('.dropdown-menu');
            
                    // Initialize the dropdown with Bootstrap's Dropdown JavaScript API
                    var wishlistDropdown = new bootstrap.Dropdown(dropdownToggle, {
                        autoClose: true, // Automatically close the dropdown after an item is selected
                        display: 'static' // Keep the dropdown open when an item is selected
                    });
                });
            </script>
            
            <style>
                .modify-order-btn {
                    background-color: #A0522D; /* Coffee themed button */
                    border-color: #A0522D;
                }

                .modify-order-btn:hover {
                    background-color: #8B4513; /* Darker shade on hover */
                    border-color: #8B4513;
                }

                .remove-from-cart-btn {
                    background-color: #8B0000; /* Coffee themed button */
                    border-color: #8B0000;
                }

                .remove-from-cart-btn:hover {
                    background-color: #800000; /* Darker shade on hover */
                    border-color: #800000;
                }
            </style>
            <!-- Cart Management -->
            <section class="container cart-management" style="background: linear-gradient(to right, #CD853F, #D2691E, #CD853F); border: 2px solid #8B4513; padding: 20px; margin-top: 200px;">
                <h2 style="background: linear-gradient(to right, #FFF8DC, #F5DEB3, #FFF8DC); border-bottom: 1px solid #8B4513; padding: 10px; border-radius: 10px; max-width: 200px;">Cart Management</h2>
                <div class="cart">
                    <!-- Cart Items -->
                    <div class="cart-item" style="background-color: #D2B48C; padding: 10px; margin-bottom: 10px; max-width: 200px; border-radius: 10px;">
                        <p style="font-size: medium;">Product Name</p>
                        <button class="btn btn-primary modify-order-btn">Modify Order</button>
                    </div>
                    <!-- More Cart Items... -->
                    <!-- Add to Cart Button -->
                    <button class="btn btn-danger remove-from-cart-btn">Remove from Cart</button>
                </div>
            </section>
        </main>

        <footer>
            <style>
                #logoutButton {
                position: absolute;
                top: 10px; /* Adjust the top position as needed */
                right: 367px; /* Adjust the right position as needed */
                z-index: 9999; /* Ensure it's above other content */
            }
            </style>
        <!-- Logout Button -->
        <button id="logoutButton" class="btn btn-danger">
            <span class="bi bi-arrow-left"></span>
        </button>
        
        </footer>
        <script>
            // JavaScript code to navigate back to the profile page when logout button is clicked
            document.getElementById("logoutButton").addEventListener("click", function() {
                // Redirect to the profile page
                window.location.href = "/logged_in.html";
            });
        </script>
    <nav class="window" style="width: 200px; height: 200px; border: 1px, solid, black">
        <script>
            const menuButton = document.getElementById('menuButton');
            const menuContent = document.querySelector('.menu-content');
            const uploadForm = document.getElementById('profilePictureForm'); // Corrected the form ID
            const imageInput = document.getElementById('profilePicture'); // Corrected the input ID
            menuButton.addEventListener('click', toggleMenu);
        
            function toggleMenu() {
                menuContent.classList.toggle('show');
            }
        
            const menu = document.getElementById('menu');
            const content = document.querySelector('.content');
        
            let isMenuOpen = false;
        
            content.addEventListener('touchstart', handleTouchStart, false);
            content.addEventListener('touchmove', handleTouchMove, false);
        
            let xDown = null;
            let yDown = null;
        
            function handleTouchStart(event) {
                xDown = event.touches[0].clientX;
                yDown = event.touches[0].clientY;
            }
        
            function handleTouchMove(event) {
                if (!xDown || !yDown) {
                    return;
                }
        
                let xUp = event.touches[0].clientX;
                let yUp = event.touches[0].clientY;
        
                let xDiff = xDown - xUp;
                let yDiff = yDown - yUp;
        
                if (Math.abs(xDiff) > Math.abs(yDiff)) { /* Most significant */
                    if (xDiff > 0) {
                        /* left swipe */
                        closeMenu();
                    } else {
                        /* right swipe */
                        openMenu();
                    }
                }
        
                /* reset values */
                xDown = null;
                yDown = null;
            }
        
            function openMenu() {
                menu.style.right = '0';
                isMenuOpen = true;
            }
        
            function closeMenu() {
                menu.style.right = '-200px';
                isMenuOpen = false;
            }
                
            uploadForm.addEventListener('submit', (event) => {
                event.preventDefault();
                const formData = new FormData();
                formData.append('profilePicture', imageInput.files[0]); // Append the file input
                
                fetch('/profile-photo', {
                    method: 'POST',
                    body: formData
                })
                .then(response => response.json())
                .then(data => {
                    document.getElementById('profileImage').src = data.profilePictureUrl;
                })
                .catch(error => {
                    console.error('Error uploading profile picture:', error);
                });
            });

            function uploadProfilePicture() {
                const formData = new FormData(uploadForm);
                fetch('/profile-photo', {
                    method: 'POST',
                    body: formData
                })
                .then(response => response.json())
                .then(data => {
                    document.getElementById('profileImage').src = data.profilePictureUrl;
                })
                .catch(error => {
                    console.error('Error uploading profile picture:', error);
                });
            }

            // Wait for the DOM to be fully loaded
            document.addEventListener("DOMContentLoaded", function() {
                // Get the configuration icon element
                const configIcon = document.querySelector('.config-icon');

                // Add a click event listener to the configuration icon
                configIcon.addEventListener('click', function(event) {
                    // Prevent the default action of the click event
                    event.preventDefault();

                    // Toggle the dropdown menu when the configuration icon is clicked
                    const dropdownMenu = this.querySelector('.dropdown-menu');
                    const isOpen = new bootstrap.Dropdown(dropdownMenu).toggle();

                    // Focus on the dropdown toggle button after toggling the dropdown
                    if (!isOpen) {
                        this.querySelector('.dropdown-toggle').focus();
                    }
                });
            });
        </script>
</body>
</html>